# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/
jspm_packages/

# Optional npm cache directory
.npm

# Output directory for Vite build
dist/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# SQLite journal files (but keep the database.db seed in git if needed, or ignore it if using persistent volumes)
db/*.db-journal
db/*.db-shm
db/*.db-wal
